import nacl from"tweetnacl";import jsonfile from"jsonfile";import{dirname,resolve}from"path";import{fileURLToPath}from"url";import{writeFileSync}from"node:fs";const __dirname=dirname(fileURLToPath(import.meta.url)),keypair=nacl.sign.keyPair(),publicKey=Buffer.from(keypair.publicKey).toString("hex"),secretKey=Buffer.from(keypair.secretKey).toString("hex");jsonfile.writeFileSync(resolve(__dirname,"..","const.json"),{secretKey,publicKey}),writeFileSync(resolve(__dirname,"..","pubkey"),publicKey);
